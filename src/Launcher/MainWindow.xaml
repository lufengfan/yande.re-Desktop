<ctrlib:DropShadowWindow x:Class="Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Launcher"
        xmlns:ctrlib="clr-namespace:Launcher.Controls;assembly=Launcher.ctrlib"
        mc:Ignorable="d"
        Title="yande.re"
        AllowsTransparency="True" WindowStyle="None"
        Background="Transparent"
        BorderBrush="LightBlue" BorderThickness="1"
        Height="500" Width="1000"
        MinWidth="600" MinHeight="300"
        BlurRadius="5" DropShadowColor="LightBlue">
    <ctrlib:DropShadowWindow.Style>
        <Style TargetType="ctrlib:DropShadowWindow">
            <Style.Triggers>
                <Trigger Property="Window.IsActive" Value="False">
                    <Setter Property="Window.BorderThickness" Value="0"/>
                    <Setter Property="ctrlib:DropShadowWindow.DropShadowColor" Value="Transparent"/>
                </Trigger>
                <Trigger Property="Window.WindowState" Value="Maximized">
                    <Setter Property="Window.MaxHeight" Value="{DynamicResource {x:Static SystemParameters.MaximizedPrimaryScreenHeightKey}}"/>
                    <Setter Property="Window.BorderThickness" Value="0"/>
                    <Setter Property="ctrlib:DropShadowWindow.DropShadowColor" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </ctrlib:DropShadowWindow.Style>
    
    <Grid
            Background="#FF2F2F2F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label x:Name="TitleRect" Grid.Row="0"
                Height="30"
                VerticalContentAlignment="Center"
                Foreground="Gray" FontSize="16"
                Content="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}, Path=Title, Mode=OneWay}"
                ctrlib:CustomizedWindow.CustomizedWindowAncestor="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}, Mode=OneWay}"
                ctrlib:CustomizedWindow.IsWindowTitleAreaPart="True"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Visibility="Visible">
            <WrapPanel x:Name="wp" Orientation="Horizontal" HorizontalAlignment="Stretch"
                       Loaded="wp_Loaded">
                <WrapPanel.Resources>
                    <Style TargetType="ctrlib:PostThumb" x:Key="VerticalOrientedPostThumbStyle">
                        <Setter Property="Height" Value="160"/>
                        <Setter Property="Background" Value="#1FFFFFFF"/>
                        <Setter Property="Margin" Value="8,10"/>
                        <Setter Property="CornerRadius" Value="5"></Setter>
                    </Style>
                </WrapPanel.Resources>

                <ctrlib:FilletPanel Height="auto" CornerRadius="20">
                </ctrlib:FilletPanel>
                <ctrlib:FilletPanel Height="400">
                </ctrlib:FilletPanel>
                <ctrlib:ChamferPanel Height="400" CornerSize="10">
                </ctrlib:ChamferPanel>
                <ctrlib:FilletPanel Height="400">
                </ctrlib:FilletPanel>
                <ctrlib:FilletPanel Height="400">
                </ctrlib:FilletPanel>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</ctrlib:DropShadowWindow>
